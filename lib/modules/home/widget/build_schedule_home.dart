import 'package:flutter/material.dart';

class BuildClosestSchedule extends StatelessWidget {
  const BuildClosestSchedule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
